# ── Required (Phase 0) ──────────────────────────────────────────────
DATABASE_URL="postgresql://user:password@host:5432/stratum?schema=public"
SERVER_PORT=4000
CLIENT_URL="http://localhost:5173"

# ── Auth (Phase 2 — required at boot) ───────────────────────────────
JWT_SECRET="generate-with: openssl rand -hex 32"

# ── Cloudinary (Phase 3 — placeholder, not yet validated at boot) ───
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""
