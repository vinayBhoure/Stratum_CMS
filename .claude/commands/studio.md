Open Prisma Studio to visually browse and edit the Stratum CMS database.

1. Navigate to `/server`
2. Run `npx prisma studio`
3. Confirm it opens on the default port (usually 5555)
4. Remind the developer: Prisma Studio is for debugging — do not use it to make production data changes
